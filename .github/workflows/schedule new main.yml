name: Schedule Test

on:
  schedule:
    - cron: "45 8 * * *"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Schedule Trigger Success
        run: |
          echo "✅ Schedule workflow triggered successfully"
          echo "⏰ Test time: 2:15 PM IST"
          date
